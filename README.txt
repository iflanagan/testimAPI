Edit the MyConfiguration.java class and add your testim API key and projectID
Save the file

THe main class that is working is MyFunctions.java 

Each API operation uses a slightly different URL

MyFunctions.buildURL will build the URL then each method that performs an API operation calls that method 
